# Prompt: Repo Init

```text
Use the `repo-init` skill.
Initialize this repository for AI-agent work. Create only missing files and folders. Do not overwrite existing AGENTS.md, CLAUDE.md, .ai files, or existing project notes.
```
