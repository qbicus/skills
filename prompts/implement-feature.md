# Prompt: Implement Feature

Use when starting a feature from a request.

```text
Use the `new-feature` skill for this feature.
Start with the spec stage only. Do not implement until the spec, design, and task plan are approved.
```
