# Prompt: Session Close

```text
Run the `session-close` skill.
Update current work, decisions if needed, blockers, next steps, task status, and restart notes. Do not start new implementation work.
```
