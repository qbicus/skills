# Git Rules

- Do not overwrite existing project AI files during initialization unless explicitly requested.
- Do not auto-commit unless the user explicitly asks.
- Keep generated session files readable in diffs.
- Prefer small commits grouped by concern.
- Before switching projects, run `session-close` if meaningful work happened.
